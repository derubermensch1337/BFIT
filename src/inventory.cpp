#include "Arduino.h"
#include "inventory.h"

struct user {
    char name[25];
    char room[3];
    uint8_t green;
    uint8_t clasic;
    uint8_t sommersby;
    uint8_t limfjords_porter;
    uint8_t cola;
    uint8_t cola_zero;
};


struct inventory {
    /* data */
};
